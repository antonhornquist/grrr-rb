require 'rake/testtask'

task :default => :tests

task :tests => [:test_grrr, :test_unstable, :test_scext]

Rake::TestTask.new(:test_grrr) do |t|
	t.test_files = ["test/tc_grrr.rb"]
	t.warning = true
end

Rake::TestTask.new(:test_unstable) do |t|
	t.test_files = ["test/tc_unstable.rb"]
	t.warning = true
end

Rake::TestTask.new(:test_scext) do |t|
	t.test_files = ["test/tc_scext.rb"]
	t.warning = true
end

task :irb => :environment do
	require 'irb'
	ARGV.clear
	IRB.start
end

task :environment do
	require 'grrr'
	require 'unstable'
	require 'screengrid' if (defined? RUBY_ENGINE != nil) and RUBY_ENGINE == 'jruby'
end
